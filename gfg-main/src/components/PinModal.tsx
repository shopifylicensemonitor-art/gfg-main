/**
 * PinModal.tsx — DEPRECATED.
 *
 * PIN authentication has been removed in favor of multi-tenant workspace sessions with HttpOnly cookies.
 * This file is retained as an empty shell to prevent import errors in legacy references.
 */

export default function PinModal() {
  return null;
}
